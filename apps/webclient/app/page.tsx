import { Button } from "@repo/ui/components/button";

export default function Page() {
  return (
    <div className="flex h-screen w-screen items-center justify-center">
      <Button variant={"secondary"}>WebClient</Button>
    </div>
  );
}
